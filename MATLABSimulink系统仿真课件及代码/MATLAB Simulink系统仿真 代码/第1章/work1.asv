% 第一题
clc,clear,close all
A = [6 3 4;
    -2,5,7;
    8,-1,-3];  % 左边系数
B = [3;-4;-7]; % 方程右边系数
% (1)方程的解
x = inv(A)*B
% (2)矩阵的秩
r = rank(A)
% (3)矩阵的特征值与特征向量
[v,lambda] = eig(A)
% (4)矩阵的 

